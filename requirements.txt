# Root requirements — combined from all sub-projects
# NOTE: Each sub-project has its own requirements.txt. Use those when working in isolation.
# This file lists all dependencies together for reference.
#
# POTENTIAL CONFLICT: numpy, scipy, scikit-learn, matplotlib versions may differ slightly
# between sub-projects. If you encounter version conflicts, use a separate virtualenv
# per sub-project and install from each sub-project's requirements.txt.

# ── Wasserstein_Distance_for_Time_Series-Data (Wasserstein K-Means) ────────
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0
scikit-learn>=1.0.0
POT>=0.8.0
yfinance>=0.1.70
hmmlearn>=0.2.7
tqdm>=4.62.0

# ── ECGTransForm ────────────────────────────────────────────────────────────
torch>=1.12.0
torchvision>=0.13.0
openpyxl>=3.0.0
# matplotlib, numpy, pandas, scikit-learn already listed above

# ── TiOT ────────────────────────────────────────────────────────────────────
# TiOT has no separate requirements.txt. Based on code inspection:
# numpy, scipy, scikit-learn, matplotlib already listed above
